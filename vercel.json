{
    "buildCommand": "cd react && npm install && npm run build",
    "outputDirectory": "react/dist",
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "server/main.py"
      },
      {
        "src": "/(.*)",
        "dest": "react/dist/$1"
      }
    ]
  }
  